import { GlobalClickDirective } from './global-click.directive';

describe('GlobalClickDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalClickDirective();
    expect(directive).toBeTruthy();
  });
});
